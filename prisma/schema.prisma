generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model users {
  id       Int     @id
  name     String
  telefono String?
  password String
  email    String  @unique
  rol      String
}
